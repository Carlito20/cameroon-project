// Set to true to disable ordering — customers can browse but not add to cart or checkout.
// Flip to false and redeploy when products are on-site and you're ready to open.
export const VIEW_ONLY = false;

export const VIEW_ONLY_MESSAGE = "Our products are on their way! Browse our full collection now — ordering opens once stock arrives.";

// Set to true to hide numeric prices everywhere on the public site (shop grid,
// product modal, homepage, basket, WhatsApp inquiry message, order confirmation)
// while prices are still being finalized. Admin dashboard, price-tags printing,
// and the underlying database are unaffected — only customer-facing display.
// Flip to false and redeploy when prices are ready to go public.
export const HIDE_PRICES = true;

export const HIDE_PRICES_MESSAGE = "Price coming soon";
